helm repo add bitnami https://charts.bitnami.com/bitnami

helm install clbr-rabbitmq bitnami/rabbitmq --namespace rabbitmq --values bitnami-legacy-fix.yaml


# bitnami-legacy-fix.yaml
global:
  security:
    allowInsecureImages: true
image:
  registry: docker.io
  repository: bitnamilegacy/rabbitmq  # Note: bitnamilegacy instead of bitnami
  tag: "4.1.3-debian-12-r1"  # Use a known working legacy tag
  pullPolicy: IfNotPresent

replicaCount: 1
auth:
  username: "training-user"
  password: "training-password-123"
  erlangCookie: "my-secret-cookie"

persistence:
  enabled: false  # Disable for initial test

service:
  type: NodePort
  nodePorts:
    amqp: "30001"
    management: "30002"

resources:
  requests:
    memory: "256Mi"
    cpu: "100m"